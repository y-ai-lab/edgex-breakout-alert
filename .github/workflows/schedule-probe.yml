name: EdgeX schedule probe

on:
  schedule:
    - cron: "4-59/5 * * * *"
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Report scheduler
        run: echo "schedule probe fired at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
